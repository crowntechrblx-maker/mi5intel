<%- include('partials/header', { pageTitle: 'Intelligence Desk', page: 'dashboard' }) %>

<div class="govuk-page-header">
  <div>
    <span class="govuk-caption-m">HM GOVERNMENT INTELLIGENCE DATABASE</span>
    <h1 class="govuk-heading-xl">Intelligence Desk</h1>
  </div>
  <div class="govuk-page-header__actions">
    <a href="/watchlist/batch" class="govuk-button govuk-button--secondary">BATCH UPLOAD</a>
    <a href="/watchlist/add" class="govuk-button govuk-button--primary">+ Register Entity</a>
  </div>
</div>

<div class="stats-grid stats-grid--5">
  <div class="stat-card">
    <div class="stat-value"><%= stats.total %></div>
    <div class="stat-label">TOTAL ENTITIES</div>
  </div>
  <div class="stat-card stat-card--critical">
    <div class="stat-value"><%= stats.critical %></div>
    <div class="stat-label">CRITICAL SEVERITY</div>
  </div>
  <div class="stat-card stat-card--high">
    <div class="stat-value"><%= stats.high %></div>
    <div class="stat-label">HIGH SEVERITY</div>
  </div>
  <div class="stat-card stat-card--medium">
    <div class="stat-value"><%= stats.medium %></div>
    <div class="stat-label">MEDIUM SEVERITY</div>
  </div>
  <div class="stat-card stat-card--active">
    <div class="stat-value"><%= stats.active %></div>
    <div class="stat-label">ACTIVE STATUS</div>
  </div>
</div>

<div class="dashboard-grid">
  <div class="govuk-panel">
    <div class="govuk-panel__header">
      <span class="govuk-panel__title">RECENTLY REGISTERED ENTITIES</span>
      <a href="/watchlist" class="govuk-panel__link">VIEW ALL &rarr;</a>
    </div>
    <div class="govuk-panel__body govuk-panel__body--flush">
      <% if (recent.length === 0) { %>
        <div class="empty-state">No entities registered yet. <a href="/watchlist/add">Register the first entity.</a></div>
      <% } else { %>
        <% recent.forEach(function(e) { %>
        <a href="/watchlist/<%= e.id %>" class="entity-row">
          <div class="entity-avatar">
            <% if (e.avatar_url) { %><img src="<%= e.avatar_url %>" alt=""><% } else { %><span><%= e.username.charAt(0).toUpperCase() %></span><% } %>
          </div>
          <div class="entity-info">
            <div class="entity-username"><%= e.username %></div>
            <div class="entity-meta">RBX: <%= e.roblox_id %><% if (e.category) { %> &bull; <%= e.category %><% } %></div>
          </div>
          <div class="entity-badges">
            <strong class="govuk-tag govuk-tag--sev-<%= e.severity.toLowerCase() %>"><%= e.severity %></strong>
            <strong class="govuk-tag govuk-tag--status-<%= e.status.toLowerCase() %>"><%= e.status %></strong>
          </div>
        </a>
        <% }); %>
      <% } %>
    </div>
  </div>

  <div class="govuk-panel">
    <div class="govuk-panel__header">
      <span class="govuk-panel__title">RECENT AUDIT ACTIVITY</span>
      <a href="/audit" class="govuk-panel__link">VIEW ALL &rarr;</a>
    </div>
    <div class="govuk-panel__body govuk-panel__body--flush">
      <% if (recentLogs.length === 0) { %>
        <div class="empty-state">No audit events recorded.</div>
      <% } else { %>
        <% recentLogs.forEach(function(log) { %>
        <div class="audit-row">
          <span class="audit-action-badge"><%= log.action.replace(/_/g, ' ') %></span>
          <span class="audit-actor"><%= log.actor %></span>
          <% if (log.target) { %><span class="audit-target"><%= log.target %></span><% } %>
          <span class="audit-time"><%= new Date(log.created_at).toLocaleString('en-GB') %></span>
        </div>
        <% }); %>
      <% } %>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
